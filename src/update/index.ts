import { update as pickup } from './pickup'
import { update as roll } from './roll'
import { update as replace } from './replace'
import { update as roundEnd } from './round-end'

export = {
  pickup,
  roll,
  replace,
  roundEnd,
}
